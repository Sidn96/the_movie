import 'package:flutter/widgets.dart';

class AppRadius {
  AppRadius._();

  /// Radius of 0.
  static const none = BorderRadius.zero;

  /// Extra extra small radius of 2.
  static BorderRadius xxs = BorderRadius.circular(2);

  /// Extra small radius of 4.
  static BorderRadius xs = BorderRadius.circular(4);

  /// Small radius of 6.
  static BorderRadius sm = BorderRadius.circular(6);

  /// Large radius of 8.
  static BorderRadius lg = BorderRadius.circular(8);

  /// Large radius of 12.
  static BorderRadius xlg = BorderRadius.circular(12);

  /// Extra extra large radius of 16.
  static BorderRadius xxlg = BorderRadius.circular(16);
}
